# VC0008

Provenance: transcribed from the canonical `R0 !read_ok` shape; same wording
as the kernel verifier's "return register not initialised on this path"
diagnostic. Models a Horizon `match` arm that returns without yielding a
value.
