[
  {
    "code": "HZN3110",
    "severity": "error",
    "message": "Helper return dereferenced without a nil check",
    "primary": {
      "start": {
        "line": 0,
        "column": 0
      },
      "end": {
        "line": 0,
        "column": 0
      }
    },
    "notes": [
      "verifier-catalog: VC0001",
      "cause: The verifier rejects a load through a pointer whose nullability has not been proven. This typically means a helper return (bpf_map_lookup_elem, bpf_ringbuf_reserve, bpf_sk_lookup_tcp, etc.) was dereferenced before a nil check on every path. The verifier tracks the helper return as a scalar until it is compared against NULL.",
      "; let reservation = bpf.ringbuf.reserve(events, 64);\n; *reservation = payload;\ninvalid mem access 'scalar'"
    ],
    "suggest": "Insert a nil guard before dereferencing the helper's return: in .hzn, use a `match` arm or `if x != nil { ... }` and reference the checked local. If the Horizon validator did not flag this in source, the alias graph (Phase 1 #1) missed the binding — file a bug."
  }
]
