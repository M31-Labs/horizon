package main

// DiffRegistries is filled out in Task 4 Step 4.3. The Step 4.2 commit
// only needs the symbol to exist so the package builds for the parser
// tests; the stub returns "" so `check` against the on-disk registry
// passes once parsing succeeds.
//
// Replaced in Step 4.3 by a real unified-diff implementation backed by
// diff_test.go.
func DiffRegistries(left, right []RegistryHelper) string {
	return ""
}
